export enum CompletionType {
  /** The completion will be a static array of possibilities. */
  Simple = 'Simple',
  /** The completion will be retrieved by importing a CommonJS module. */
  Module = 'Module'
}
